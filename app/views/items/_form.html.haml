= simple_form_for([campaign, item]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :price
    = f.input :sold
    = f.association :hero
    = f.input :description

  .form-actions
    = f.button :submit, class: 'btn btn-success'
